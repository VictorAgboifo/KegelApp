import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.victoragboifo.kegelapp',
  appName: 'demo-ionic-angular',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;