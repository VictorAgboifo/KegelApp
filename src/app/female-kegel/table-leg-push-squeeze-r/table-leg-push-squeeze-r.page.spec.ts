import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TableLegPushSqueezeRPage } from './table-leg-push-squeeze-r.page';

describe('TableLegPushSqueezeRPage', () => {
  let component: TableLegPushSqueezeRPage;
  let fixture: ComponentFixture<TableLegPushSqueezeRPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(TableLegPushSqueezeRPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
