import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TableLegPushSqueezeLPage } from './table-leg-push-squeeze-l.page';

describe('TableLegPushSqueezeLPage', () => {
  let component: TableLegPushSqueezeLPage;
  let fixture: ComponentFixture<TableLegPushSqueezeLPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(TableLegPushSqueezeLPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
